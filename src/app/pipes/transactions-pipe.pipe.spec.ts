import { TransactionsPipePipe } from './transactions-pipe.pipe';

describe('TransactionsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
