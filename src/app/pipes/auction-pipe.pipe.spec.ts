import { AuctionPipePipe } from './auction-pipe.pipe';

describe('AuctionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AuctionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
