import { OwnerPipePipe } from './owner-pipe.pipe';

describe('OwnerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OwnerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
