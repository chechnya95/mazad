import { DepositPipePipe } from './deposit-pipe.pipe';

describe('DepositPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DepositPipePipe();
    expect(pipe).toBeTruthy();
  });
});
