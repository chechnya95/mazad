import { WalletPipePipe } from './wallet-pipe.pipe';

describe('WalletPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WalletPipePipe();
    expect(pipe).toBeTruthy();
  });
});
